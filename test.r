lambda <- 2  # this line sets the variable "lambda" equal to 2 TimeSteps <- 10 # sets the variable "TimeSteps" equal to 10 t <- c(1:TimeSteps) # creates a vector from 1 to the number of timesteps.  Could also use the seq(1,TimeSteps,1) command
N <- numeric() # creates an empty numeric vector

print(lambda)